# 🔧 Troubleshooting & FAQ

## 🚨 Common Issues & Solutions

---

## 8️⃣ CORS Issues

### ❌ Error: "CORS policy blocked"

**Cause**: Frontend (React/Vue) terblokir CORS

**Solution**:
```bash
composer require fruitcake/laravel-cors
```

Edit `config/cors.php`:
```php
'paths' => ['api/*'],
'allowed_origins' => ['http://localhost:3000'],
```

---

## 📋 Quick Diagnostics Checklist

Ketika ada error, cek hal-hal ini:

### ✅ Environment
- [ ] `.env` file exists
- [ ] `APP_KEY` generated
- [ ] `JWT_SECRET` generated
- [ ] Database credentials correct

### ✅ Database
- [ ] MySQL service running
- [ ] Database created
- [ ] Migrations run
- [ ] Seeders run (optional)

### ✅ Storage
- [ ] Symbolic link created
- [ ] Permissions set correctly
- [ ] Directory exists

### ✅ Server
- [ ] Server running (`php artisan serve`)
- [ ] No port conflicts
- [ ] Routes registered (`php artisan route:list`)

### ✅ Authentication
- [ ] User registered
- [ ] Login successful
- [ ] Token copied to Postman
- [ ] Authorization header correct

---

## 🆘 Debugging Tips

### 1. Enable Debug Mode

`.env`:
```env
APP_DEBUG=true
LOG_LEVEL=debug
```

### 2. Check Logs

```bash
tail -f storage/logs/laravel.log
```

### 3. Clear Cache

```bash
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear
```

### 4. Optimize

```bash
php artisan optimize
```

### 5. Test Routes

```bash
php artisan route:list | grep products
```

---

## 💡 Performance Tips

### 1. Database Indexing

```php
// Add index for frequently queried columns
$table->index('category');
$table->index('is_featured');
```

### 2. Eager Loading

```php
// Untuk future relationships
Product::with('reviews')->get();
```

### 3. Caching

```php
use Illuminate\Support\Facades\Cache;

$products = Cache::remember('products', 3600, function () {
    return Product::all();
});
```

### 4. Query Optimization

```php
// ✅ Good
Product::select('id', 'name', 'price')->get();

// ❌ Bad (load all columns)
Product::all();
```

---

## 📞 Getting Help

### 1. Laravel Documentation
https://laravel.com/docs/11.x

### 2. JWT Auth Documentation
https://jwt-auth.readthedocs.io/

### 3. Stack Overflow
- Tag: `laravel`, `jwt`, `laravel-11`
- Search before asking

### 4. Laravel Community
- Laravel News
- Laracasts Forum
- Laravel Discord

---

## 🎓 Additional Resources

### Video Tutorials
- Laracasts: Laravel API
- Traversy Media: Laravel REST API
- Academind: Laravel Complete Guide

### Books
- Laravel: Up & Running
- Laravel Documentation (official)

### Repositories
- Laravel Boilerplates on GitHub
- JWT Auth Examples

---

## ✅ Final Checklist Before Demo

- [ ] Server running
- [ ] Database migrated & seeded
- [ ] Symbolic link created
- [ ] Can register user
- [ ] Can login & get token
- [ ] Can create product with file
- [ ] Can get all products (public)
- [ ] Can get product detail (public)
- [ ] Can update product with auth
- [ ] Can delete product with auth
- [ ] File accessible via URL
- [ ] Postman collection organized
- [ ] All endpoints documented

---

## 🎯 Demo Day Tips

1. **Test Everything Before Demo**
   - Run all requests in Postman
   - Verify all responses
   - Check file uploads working

2. **Prepare Backup**
   - Export Postman collection
   - Backup database
   - Have sample files ready

3. **Common Demo Scenarios**
   - Happy path: Register → Login → Create → Update → Delete
   - Show pagination & filtering
   - Demo file upload dengan gambar menarik
   - Show error handling

4. **Presentation Tips**
   - Explain business logic
   - Explain JWT flow
   - Explain soft delete benefit
   - Show database changes in phpMyAdmin
